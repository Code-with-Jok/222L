# Deprecated: Microfrontends Setup

This repository no longer uses the previous `web + landing-page` microfrontend
setup.

## Current State

- `apps/landing-page` is the only application entrypoint.
- Local development runs on `http://localhost:3000`.
- `@vercel/microfrontends` and the `3024` proxy are no longer part of the
  active architecture.

## Replacement Documents

- `README.md`
- `docs/architecture/refactor-monorepo-structure.md`

Keep this file only as a historical note. Do not use it as an implementation
guide for the current repository state.
