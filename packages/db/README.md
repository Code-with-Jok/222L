# `@repo/db`

Scaffold-only database package.

This package is intentionally ORM-agnostic in the current refactor. It is the
shared place for future database client setup, repository contracts, and schema
bindings once the data layer is ready.
